import {
    addOperators,
    floatNumber,
    addNumber,
    result,
    dltLastCharacter,
    clear,
} from "./source/app.js";

addOperators();

floatNumber();

addNumber();

result();

dltLastCharacter();

clear();
